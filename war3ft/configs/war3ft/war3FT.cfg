// Warcraft 3 Frozen Throne Configuration File


// Saving Options

SAVE_Enabled		0		// Enables saving of experience (uses a vault, default is 0)
SQL_Enabled		0		// Save using a SQL module (a module needs to be turned on, note: to use this mp_savexp must also be 1, default is 0)
SAVE_Pruning		0		// Automatically prunes the database of old users at a mapchange (default is 0)
SAVE_Expire_Time	31		// However many days before deleting XP or pruning from the database (default is 31)
SAVE_End_Round		1		// Save xp at the end of each round (may cause lag, default is 1)
SQL_Save_By		0		// What should I save this as?  steam id = 0, IP = 1, name = 2 (SQL ONLY, default is 0)


// SQL Database Information

SQL_dbhost		"127.0.0.1"	// Host Name
SQL_dbuser		"root"		// User Name
SQL_dbpass		""		// Password
SQL_dbname		""		// Database Name
SQL_tbname		"war3users"	// Table Name


// Admin Options

ADMIN_Flag		"m"		// Admin flag required to give/take XP (default is m)


// Experience Options

mp_xpmultiplier		1.0		// set the level required to gain a level as a multiple (default 1.0)
mp_weaponxpmodifier	1		// gives additional XP for using certain weaker weapons (default is 1)
XP_Min_Players	2		// Minimum amount of players needed b4 awarding XP (default is 2)
FT_autoxp		0		// With this option set to 1, the joining players will receive the average amount of XP that all of the players have (SHORT TERM XP ONLY, default is 0)
XP_Show_Objectives	1		// If enabled, will display message in chat to show XP they gain for completing certain objectives (default is 1)
XP_Show_Kill_Objectives	0		// Shows how much XP you gain for killing someone (each kill, default is 0)
XP_Kill_Bomb_Planter	15		// XP given for planting the bomb (default is 15)
XP_Defuse_Bomb		30		// XP given for defusing the bomb (default is 30)
XP_Rescue_Hostage	25		// XP given for helping to rescue the hostages (default is 25)
XP_Kill_Rescuer		25		// XP given for killing the person rescuing the hostages (default is 25)
XP_Kill_Bomb_Carrier	25		// XP given for killing the bomb carrier (default is 25)
XP_Kill_Defuser		25		// XP given for killing the defuser (default is 25)
XP_Kill_VIP		25		// XP given for killing the VIP (default is 25)
XP_VIP_Escape		30		// XP given when the VIP escapes (default is 30)
XP_Headshot		9		// XP given for getting a headshot (default is 9)
XP_Radius		750		// Radius to give XP to teammates near where the special objective is completed (rescued hosties, bomb planted, killed vip, vip escaped, default is 750)
XP_Win_Round		35		// Experience given to the winning team (default is 35)


// Game Play Enhancements
FT_races		8		// Number of races (if races are less than 5, the second shopmenu will be disabled (4 is standard war3), default is 8)
mp_grenadeprotection	0		// Only 1 HE grenade will be allowed for purchase per round (default is 0)
FT_position		0		// 0 is for left, 1 is for right (where to put the information for those spectating, default is 0)
FT_race_icons		1		// Allow users to type /icons to show player icons above the player (default is 1)
FT_level_icons		1		// Allow users to type /icons to see player levels above the player (default is 1)
FT_buyzone		0		// Restrict purchasing shopmenu items to within the buyzone? (default is 0)
FT_buytime		0		// Restrict purchasing shopmenu items to within the buytime? (default is 0)
FT_buydead		1		// Allow users to purchause items when dead? (default is 1)
FT_show_player		1		// Show teammate and enemy information in the center of the screen when they are on target? (not for DOD, default is 1)
FT_query_client		1		// Check user's cl_minmodel cvar every second (default is 1)


// Chameleon Race Configuration Options (Race 9)
CHAM_Random		1		// Should the 9th race have random skills each round?, if so the next 4 option are irrelevant (default is 1)
CHAM_Skill1		1		// What race should the first skill be taken from? (default is 1)
CHAM_Skill2		1		// What race should the second skill be taken from? (default is 1)
CHAM_Skill3		1		// What race should the third skill be taken from? (default is 1)
CHAM_Ultimate		1		// What race should the ultimate be taken from? (default is 1)


// Skill and Ultimate configurations
ULT_Delay		15.0		// Amount of time (in seconds) to delay the ultimates at the start of each round (default is 15.0)
ULT_Cooldown		35.0		// Cooldown of all ultimates (default is 35.0)
ULT_Blink_Protection	1		// slays people who abuse blink on some maps (skywalking, map exploiting, default is 1)
ULT_Blink_Dizziness	0		// What type of diziness? (0 is flashbang effect, 1 is fading blue screen, default is 0)
ULT_Entangle_Drop	0		// Should the player's primary weapon be dropped when they are entangled? (default is 0)


// Item configurations
ITEM_Glove_Timer		10		// Interval between receiving a new grenade with the Flaming Gloves of Warmth (default is 10)
ITEM_Glove_Disable_KA	1		// Prevents Flaming gloves of warmth on ka maps and scoutzknivez (default is 1)
ITEM_Glove_Orc_Damage	0		// When using gloves, should orcs be given the critical dmg? (Default is 0)
ITEM_Claw			6		// Damage done by Claws of Attack (default is 6)
ITEM_Boots		275.0		// Speed given for purchasing boots of speed (different for dod, see dod section, default is 275)
ITEM_Health		15		// Health bonus given for periapt of health (default is 15)
ITEM_Frost		125.0		// Speed of victim when attacked with Orb of Frost (default is 125.0)
ITEM_Mask_Of_Death	0.3		// Percentage of life gained back by mask of death (default is 0.3)
ITEM_Cloak		150		// Invisibility given with cloak, lower is more invisible (default is 150)
ITEM_Sock			0.5		// Gravity level for Sock of the Feather
ITEM_Tome		50		// XP given for purchasing tome of experience (default is 50)


// Bot Configuration Options
FT_bot_buy_item		0.33		// Chance of a bot buying a shopmenu item at the beginning of the round (default is 0.33)


// DOD Options
DOD_Start_Money		800		// Starting money for dod